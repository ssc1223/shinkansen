# 本機備份
.backups/

# Node / Playwright
node_modules/
test-results/
playwright-report/
playwright/.cache/
test/reports/

# 本機打包輸出
dist/
build/
shinkansen-firefox-*.zip
shinkansen-chrome-*.zip
shinkansen-macos-*.pkg

# docs 工作區素材
docs/excluded/

# 含個人資訊的本機檔
CLAUDE.md
測試流程說明.md
CLAUDE-TEMPLATE-*.md

# 系統垃圾
.DS_Store
*.log
.env
.env.*

# debug harness 截圖與一次性 probe 腳本
.playwright-mcp/
tools/probe-*.js

# PDF layout snapshot baseline(本機素材對應)
test/regression/pdf-layout-snapshots/

# 本機規劃 / 筆記檔
PDF-TRANSLATION-PLAN.md
PLAN-send-to-instapaper.md
SPEC-PRIVATE.md
DEBUG-BOARD.md
CODE-REVIEW-2026-08-03.md
/reports/

# Claude Code 本機工作流(專案 skill / transient lock 等,含個人資訊,
# 跟 CLAUDE.md 同待遇不入公開 repo)
.claude/

# 本機商店送審工具(用個人 OAuth/API 憑證自動上架,維持本機不入公開 repo;
# 對應 ~/.shinkansen-cws-creds / ~/.shinkansen-amo-creds)
tools/chrome-webstore-submit.sh
tools/firefox-amo-submit.sh

# macOS Safari Web Extension Xcode project — Phase 2 起 commit project 骨架,
# 只 ignore build artifacts / per-user Xcode state / rsync 進來的 Extension Resources
# (對應 SPEC-PRIVATE §23.5)
safari-app/**/build/
safari-app/**/.DS_Store
safari-app/**/xcuserdata/
safari-app/Shinkansen/Shinkansen Extension/Resources/
safari-app/Shinkansen-iOS/Shinkansen/Shinkansen Extension/Resources/
safari-app/shinkansen-ios-*.ipa

# Instapaper Full API 憑證(app 層,不進 public repo)
shinkansen/lib/instapaper-keys.js

# Codex agent 本機設定(CLAUDE.md 的 Codex 鏡像,不入 repo)
AGENTS.md
.agents/

# 回歸測試套件(2026-08-06 移出公開 repo)
# 現住私有 repo github.com/jimmysu0309/Shinkansen-test,clone 回本路徑掛載——
# 路徑不變是刻意的:spec 相對路徑 / npm test / release.sh 全部不用改。
# 不用 submodule:那會在公開 repo 留下 .gitmodules 指向私有 URL。
# 重新掛載:git clone https://github.com/jimmysu0309/Shinkansen-test.git test
test/

# 內部文件與 Claude 工作流檔的疊加版控(2026-08-06 起)
# SPEC-PRIVATE.md / CLAUDE.md / AGENTS.md / .claude/ 檔案仍在原位(上方規則已 ignore),
# 版控走「疊加的第二個倉庫」:.private-git 是它的 git-dir、work-tree 指向本根目錄,
# 只追蹤上列檔案,推到私有 repo github.com/jimmysu0309/Shinkansen-docs。
# 操作一律加 --git-dir:git --git-dir=.private-git status --short / add -u / commit / push
.private-git/

# XcodeBuildMCP 專案設定（本機工具設定，不入版控）
.xcodebuildmcp/
